// Itera sobre un arreglo  e imprime cada elemento

//Inicializo los datos de entrada
let frutas = [1, 56, 89, 127];

// Inicializar el proceso del programa
for(let i = 0; i < frutas.length; i++){
    // Imprima los datos de salida
    console.log(frutas[i]);
}